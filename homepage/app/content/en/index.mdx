---
title: Proxer
description: Self-hosted reverse tunnels for HTTP, SSE, and WebSocket services.
section: overview
order: 0
layout: splash
navigation: false
---

import { ProxerHome } from "../../components/proxer-home";

<ProxerHome
  body="Expose services running on private machines through one public endpoint. Proxer keeps the wire protocol small, supports streaming and WebSockets, and ships as a native binary."
  features={["HTTP + SSE", "WebSocket", "Single-port server"]}
  getStartedLabel="Get started"
  getStartedPath="/en/getting-started/"
  installLabel="Install Proxer"
  tagline="A small reverse tunnel you can run yourself."
  terminalLabel="Proxer tunnel example"
/>
