name: proxer
description: Self-hosted reverse-tunnel CLI for HTTP, SSE, and WebSocket services.
version: 0.14.0
publish_to: none
resolution: workspace
environment:
  sdk: ^3.12.0

executables:
  proxer: proxer

dependencies:
  cliweave: ^0.1.0
  path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
